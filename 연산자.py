# 산술 연산자# 사칙 연산자 :+ ,-,*,/# // :몫# ** : 제곱 연산자# % : 나머지 연산자i = 10  # 파이썬에서는 데이터 형이 존재 Xj = 3print(f"덧셈 : {i + j}")print(f"뺄셈 : {i - j}")print(f"곱셈 : {i * j}")print(f"나눗셈 : {i / j}")print(f"몫 : {i // j}")print(f"나머지 : {i % j}")print(f"제곱 : {i ** j}")test = "Python!"print(test + "Hello")print(test + str(100))print(test * 3)# 파이썬에는 증감 연산자가 존재Xi += 1print(f"증감연산자 시험 : {i}")# 간단 예제TAX_RATE = 0.10  # 파이썬에는 상수가 존재Xincome = int(input("당신은 수업이 얼마인가요? : "))print(f"당신이 내야할 세금은 {int(income * TAX_RATE)}원 입니다.")print(f"당신이 내야할 세금은 {income * TAX_RATE:.2f}원 입니다.")  # 소수점 두자릿 수까지 표현# 관계 연산자 : && → and, || → or, ! → notx = 10y = 20z = 30rst1 = x > 0 and x > yrst2 = not (x > 0 or x > y)print(rst1)print(rst1, rst2, sep='\t')  # 사이에 두칸 띄우기# 다항 (3항) 연산자 (? ~ : ~)num = int(input("정수 입력 : "))rst = (num % 2 == 0) and "짝수" or "홀수"print(f"{num}은 {rst}입니다.")# 2진수 입력 받기 (0b)number = 0b101010101# 8진수number_8 = 0o1234567# 16진수number_16 = 0x25ff25# box